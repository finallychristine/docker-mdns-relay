[host]
# LAN interface to join for mDNS. Change this if the Mac does not use en0.
interface = en0
port = 15354
bind_address = 0.0.0.0
log_level = INFO

[client]
interface = eth0
port = 15354
server = host.docker.internal
log_level = INFO
